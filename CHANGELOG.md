# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Added `Code Quality` section to `README.md`.
- Added `CHANGELOG.md` to root and `auth` module.
- Added Prettier and ESLint configuration for code formatting and linting.

### Changed

- Configured `.prettierrc` with `tabWidth: 2`.
- Refactored `eslint.config.js` to use `eslint-plugin-prettier/recommended`.
- Reformatted codebase using Prettier.
